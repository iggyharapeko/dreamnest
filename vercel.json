{
  "version": 2,
  "buildCommand": "npm install -D tailwindcss@latest postcss@latest autoprefixer@latest --legacy-peer-deps && npm install --legacy-peer-deps && npx prisma generate && next build",
  "devCommand": "npm run dev",
  "installCommand": "npm install --legacy-peer-deps",
  "framework": "nextjs",
  "regions": ["sin1"],
  "env": {
    "NEXT_PUBLIC_API_URL": "https://${VERCEL_URL}/api"
  }
} 
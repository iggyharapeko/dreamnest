{
  "version": 2,
  "buildCommand": "npm install --legacy-peer-deps --include=dev && npx prisma generate && next build",
  "devCommand": "npm run dev",
  "installCommand": "npm install --legacy-peer-deps --include=dev",
  "framework": "nextjs",
  "regions": ["sin1"],
  "env": {
    "NEXT_PUBLIC_API_URL": "https://${VERCEL_URL}/api"
  }
} 